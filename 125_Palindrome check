class Solution:
    def isPalindrome(self, s: str) -> bool:
        pcheck = []
        for i in s:
            if i.lower().isalnum():
                pcheck.append(i.lower())
                
        if pcheck[:] == pcheck[::-1]:
            return True
        else:
            return False
        
